{
  "extends": "../../tsconfig.base.json",
  "//": "src/runtime is compiled by the consuming Nuxt app, where #imports resolves. Typechecking it standalone would need generated .nuxt types, so the module entry is what we check here. No `types` entry: pulling in @types/node passed locally only because pnpm hoisted it out of nuxt, and failed on a clean checkout.",
  "include": ["src/module.ts"]
}
